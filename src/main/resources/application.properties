spring.application.name=weather-app
# Read OpenWeather API key from env var (set this in Render)
openweather.api.key=${OPENWEATHER_API_KEY}

# Let Render control the port via the PORT env var (fallback to 8080 locally)
server.port=${PORT:8080}
